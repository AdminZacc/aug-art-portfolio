{
  "name": "aug-art-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Static art portfolio (GitHub Pages + Supabase) with PWA features.",
  "scripts": {
    "dev": "http-server -p 5173 -c-1 .",
    "start": "npm run dev",
    "build:config": "node generate-config.js",
    "prebuild": "npm run build:config",
    "predev": "npm run build:config"
  },
  "devDependencies": {
    "dotenv": "^16.6.1",
    "http-server": "^14.1.1"
  }
}
